name: Ruff

on:
  push:
    branches: [add-github-workflows]
  pull_request:
    branches: [add-github-workflows]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v1
